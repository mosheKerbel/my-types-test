import React from 'react';
import {TextInput, Button} from 'my-lib';

function App() {
  return (
    <div className="App">
      <TextInput onChange={()=>{}} onFocus={()=>{}} value={1}/>
      <Button disabled={false} skin={'alert'} onClick={()=>{}} dataHook={'f'}  />
    </div>
  );
}

export default App;
